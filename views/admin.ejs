<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理者ページ | Tiny-URL</title>
    <meta name="description" content="URLを短縮するサイトです。">
    <meta name="keywords" content="短縮,URL,URL短縮,短縮URL">
    <meta name="theme-color" content="#6E3DD9">
    <meta name="robots" content="noindex" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
            color: #e2e8f0;
        }

        .gradient-text {
            background-image: linear-gradient(90deg, #6366f1, #a855f7);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .transition-ease {
            transition: all 0.3s ease-in-out;
        }
    </style>
</head>

<body class="min-h-screen flex flex-col antialiased">
    <header class="p-4 sm:p-6 bg-slate-800/80 backdrop-blur-sm shadow-lg sticky top-0 z-10">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
            <a href="/" class="text-3xl font-extrabold gradient-text tracking-tight">Tiny-URL</a>
            <nav class="flex space-x-6 items-center">
                <a href="/" class="text-slate-300 hover:text-indigo-400 transition-ease text-sm font-medium">ホーム</a>
                <span
                    class="px-4 py-2 bg-yellow-600/20 text-yellow-400 text-sm font-semibold rounded-lg border border-yellow-600/50">管理者</span>
            </nav>
        </div>
    </header>
    <main class="flex-grow p-4 sm:p-8">
        <div class="max-w-7xl mx-auto">
            <div class="mb-8">
                <h1 class="text-4xl sm:text-5xl font-extrabold gradient-text mb-2">管理者パネル</h1>
                <p class="text-slate-400">システム全体の管理と設定</p>
            </div>
            <div class="bg-slate-800 p-6 sm:p-8 rounded-2xl shadow-xl border border-slate-700/50 mb-6">
                <h2 class="text-2xl font-bold text-white mb-6">全URL一覧</h2>
                <form method="POST" action="/admin/delete" id="deleteForm">
                    <div class="overflow-x-auto mb-4">
                        <table class="w-full text-left">
                            <thead>
                                <tr class="border-b border-slate-700">
                                    <th class="pb-3 text-slate-400 font-semibold text-sm">#</th>
                                    <th class="pb-3 text-slate-400 font-semibold text-sm">オリジナルURL</th>
                                    <th class="pb-3 text-slate-400 font-semibold text-sm">短縮URL</th>
                                    <th class="pb-3 text-slate-400 font-semibold text-sm">選択</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for(var i in content) { %>
                                    <% var obj=content[i]; %>
                                        <tr class="border-b border-slate-700/50 hover:bg-slate-700/30 transition-ease">
                                            <td class="py-4 text-slate-300">
                                                <%= Number(i)+1 %>
                                            </td>
                                            <td class="py-4 text-slate-300 max-w-md truncate">
                                                <%= obj.original %>
                                            </td>
                                            <td class="py-4">
                                                <a href="<%= obj.tiny %>" target="_blank"
                                                    class="text-indigo-400 hover:text-indigo-300 transition-ease">
                                                    <%= obj.tiny %>
                                                </a>
                                            </td>
                                            <td class="py-4">
                                                <input type="checkbox" name="delnum" value="<%= content[i].tiny %>"
                                                    class="w-4 h-4 text-indigo-600 bg-slate-700 border-slate-600 rounded focus:ring-indigo-500">
                                            </td>
                                        </tr>
                                        <% } %>
                            </tbody>
                        </table>
                    </div>
                    <button type="submit"
                        class="px-6 py-3 bg-red-600 hover:bg-red-500 text-white font-semibold rounded-xl transition-ease active:scale-[0.99]">選択したURLを削除</button>
                </form>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700/50">
                    <h3 class="text-xl font-bold text-white mb-4">一括削除</h3>
                    <form method="POST" action="/admin/alldelete">
                        <p class="text-slate-400 text-sm mb-4">データベース内のすべてのURLを削除します</p>
                        <button type="submit"
                            class="w-full py-3 bg-red-800 hover:bg-red-700 text-white font-semibold rounded-xl transition-ease active:scale-[0.99]"
                            onclick="return confirm('本当にすべてのURLを削除しますか？この操作は取り消せません。')">すべて削除</button>
                    </form>
                </div>
                <div class="bg-slate-800 p-6 rounded-2xl shadow-xl border border-slate-700/50">
                    <h3 class="text-xl font-bold text-white mb-4">プレミアムユーザー追加</h3>
                    <form method="POST" action="/admin/premiumadd" class="space-y-4">
                        <div>
                            <label class="block text-slate-400 text-sm font-medium mb-2">ユーザーID</label>
                            <input type="text" name="id" placeholder="DiscordユーザーIDを入力"
                                class="w-full p-3 bg-slate-700 border border-slate-600 text-slate-200 placeholder-slate-400 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-ease"
                                required>
                        </div>
                        <button type="submit"
                            class="w-full py-3 bg-indigo-600 hover:bg-indigo-500 text-white font-semibold rounded-xl transition-ease active:scale-[0.99]">プレミアムに追加</button>
                        <button type="submit" name="demo" value="true"
                            class="w-full py-3 bg-purple-600 hover:bg-purple-500 text-white font-semibold rounded-xl transition-ease active:scale-[0.99]">デモユーザーとして追加</button>
                    </form>
                </div>
            </div>
        </div>
    </main>
    <footer class="p-4 sm:p-6 text-center text-slate-500 text-sm bg-slate-800 border-t border-slate-700/50">
        &copy; 2025 Tiny-URL. All rights reserved.
    </footer>
    <script>
        const deleteForm = document.getElementById('deleteForm');
        deleteForm.addEventListener('submit', (e) => {
            const checkboxes = document.querySelectorAll('input[name="delnum"]:checked');
            if (checkboxes.length === 0) {
                e.preventDefault();
                alert('削除するURLを選択してください');
            }
        });
    </script>
</body>

</html>